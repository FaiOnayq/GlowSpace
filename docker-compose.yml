services:
  app:
    build: .
    container_name: glowspace
    ports:
      - "3001:3001"
    env_file:
      - .env
    volumes:
      - .:/app
      - node_modules:/app/node_modules
    command: sh -c "npm install && npm run dev"

volumes:
  node_modules: